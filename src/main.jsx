import React from "react";
import ReactDOM from "react-dom/client";
import App from "../seeded_svg_splotch_generator_single_page_app.tsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
